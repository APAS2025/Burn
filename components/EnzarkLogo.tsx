import React from 'react';

const EnzarkLogo: React.FC<{ className?: string }> = ({ className }) => (
  <div className={className} style={{ border: '2px solid red', width: '100px', height: '100px' }}>
    Logo Placeholder
  </div>
);

export default EnzarkLogo;
